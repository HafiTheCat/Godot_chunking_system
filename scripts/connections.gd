extends Node

@export_category("Include")
@export var size: Vector2i = Vector2i(1,1);
@export_group("Array")
@export var north: Array = []
@export var east: Array = []
@export var south: Array = []
@export var west: Array = []



# Called when the node enters the scene tree for the first time.
func _ready():
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
